# Firebase の設定手順

オンライン機能(ランキング・サクセス記録・所属バンド一覧)を動かすために、
Firebaseコンソールで2つ設定してください。

## 1. セキュリティルールを設定する

1. [Firebaseコンソール](https://console.firebase.google.com/) でプロジェクト `band-simulation-game` を開く
2. 左メニューの **Firestore Database** → 上部の **ルール** タブ
3. `firestore.rules` の中身をまるごと貼り付けて **公開**

これをやらないと `permission-denied` で保存も読み込みもできません。

## 2. 匿名ログインを有効にする

1. 左メニューの **Authentication** → **Sign-in method**
2. **匿名** を有効にして保存

プレイヤーの識別に匿名ログインを使っています。

## 3. (任意) 複合インデックスを作る

事務所画面の「所属バンド」は `isMajor == true` を `fame` 降順で並べます。
インデックスが無くても動きますが(自動でフォールバックします)、件数が増えたら作ると速くなります。

- コレクション: `completedRuns`
- フィールド: `isMajor` (昇順) + `fame` (降順)

コンソールの警告リンクからワンクリックで作成できます。

## 使っているコレクション

| コレクション | 用途 |
|---|---|
| `players` | 進行中のプロフィール。フレンド検索に使う |
| `completedRuns` | サクセスを完走した記録。ランキングと所属バンド一覧の元データ |
| `friendRequests` | フレンド申請 |
| `friends` | 成立したフレンド |
